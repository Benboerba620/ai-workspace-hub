# Rules 兼容入口

规则现在采用“一条规则一张卡”，存放在 `wiki/rules/`；短摘要由
`wiki/rules/_index.md` 自动生成。这样每条规则都可以独立加载、复审、
降级、废弃或被新规则替代。

旧工作区若已在本文件积累规则，不会被自动迁移或覆盖。请在复盘时逐条
转成 `wiki/rules/_template.md` 格式，确认无遗漏后再保留本文件作为入口。

<!-- 文件说明：旧版单文件规则表的兼容入口。 -->
