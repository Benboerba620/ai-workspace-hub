# Pattern 旧路径说明

Pattern 已归入知识库的 `wiki/patterns/`。新建卡片和加载索引都使用：

- `wiki/patterns/_template.md`
- `wiki/patterns/_index.md`

旧工作区里已经存在的 `workspace/patterns/*.md` 仍会被生命周期脚本读取，
但不会自动移动，避免升级时改动用户文件。

<!-- 文件说明：preview 版本旧路径的兼容说明。 -->
